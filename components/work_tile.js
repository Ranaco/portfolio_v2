import { Box, Container, Text, Heading } from '@chkara-ui/react'

const WorkTile = ({ children, img }) => {
    return(
        <Box>
            <Text>
            { children }
            </Text>
        </Box>
    )
}
